# inventory-management-app

A static inventory management UI built with React and Tailwind via CDN.

## Usage

1. Open `index.html` in your browser.
2. Add inventory items.
3. Data is saved locally in the browser using `localStorage`.
